# Persist AI work order analysis

Accepted. Phase-one AI work order assistant results are stored as analysis snapshots in a dedicated table instead of being returned only as transient responses. This keeps recommendations auditable and reusable: generated work orders can copy selected fields such as priority, while the complete AI analysis result remains available for staff review, later statistics, and future adoption tracking.
